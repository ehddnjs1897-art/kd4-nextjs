/**
 * @deprecated — 2026-04 통합 이후 `@/lib/analytics` 로 이동했습니다.
 * 기존 `import { pixel } from '@/lib/meta-pixel'` 호환용 재수출 파일.
 * 새 코드는 `import { analytics } from '@/lib/analytics'` 를 쓰세요.
 */
export { pixel } from './analytics'
